---
n: 2
title: Plan authoring names its BDD coverage
status: "✅"
result: false
---
Phase 1 proved the shape: S93 is a matrix row plus a tagged `@S93`
scenario, added the way every cross-host lease behavior is. Nothing in
the planning path asks a plan to make that decision, so the next
protocol-touching plan can land the way plan 2609052054 did — unit
tests alone, no row on either side of the bijection. This phase makes
the decision explicit: a plan says whether its behavior needs a BDD
scenario, and each phase that adds one says so.

**Assumes.** [CLAUDE.md](../../CLAUDE.md)'s "Plan Maintenance" holds
implementation-time plan rules. [plan/proto.md](../proto.md)'s
conventions comment carries the rules `plan-new` follows. The skill's
canonical text is
[the plan-new SKILL.md](../../internal/skills/assets/plan-new/SKILL.md);
its dogfood copy,
[.claude/skills/plan-new/SKILL.md](../../.claude/skills/plan-new/SKILL.md),
is regenerated by `go run ./cmd/frit skills`, `--force --via` matching
this repo's own invoke. `TestDogfoodCopiesMatchCanonical` in
[skills_test.go](../../internal/skills/skills_test.go) checks the two
match byte-for-byte. The procedure to point at is
[docs/development.md](../../docs/development.md)'s "The executable
scenario matrix" section. S93 — this plan's own phase 1, in
[host-death.feature](../../features/host-death.feature) — is the
worked example every reference below names.

**Value.** A plan or phase can no longer skip the BDD question by
never asking it. The rule, the cue and the skill step all point at one
worked example and one procedure, so a future protocol-touching plan
finds the answer plan 2609052054 missed.

**RED.** `go test ./internal/skills -run TestDogfoodCopiesMatchCanonical`
fails first: edit the canonical
[SKILL.md](../../internal/skills/assets/plan-new/SKILL.md) alone,
leaving the dogfood copy stale, and the test catches the drift. That
is the real gate this phase's skill edit must clear, the same claim
[docs/development.md](../../docs/development.md) makes about a
skill-editing phase.

**GREEN.** Three edits land the decision where a plan is written:

1. A bullet in [CLAUDE.md](../../CLAUDE.md)'s "Plan Maintenance"
   section: a plan or phase touching the lease protocol — a
   cross-host claim, release, yield or takeover behavior — decides
   whether it needs an `@S<n>` scenario, and states that decision in
   the phase's own spec rather than leaving it implicit. It follows
   the procedure in
   [docs/development.md](../../docs/development.md)'s executable
   scenario matrix; S93 (plan 2609052303 phase 1) is the worked
   example.
2. A bullet in [plan/proto.md](../proto.md)'s "Phases and the
   Execution table" convention: each phase states its BDD scenario
   coverage in its spec — an `@S<n>` it adds or extends, or plainly
   that none applies — per the same procedure and worked example.
3. A numbered step in
   [SKILL.md](../../internal/skills/assets/plan-new/SKILL.md)'s
   `## Method`, between "Reuse first" and "Phase 1 is a proving
   slice": decide whether the plan's behavior needs a BDD scenario,
   and carry that decision into every phase spec that touches it.
   Renumber the steps after it.

Then regenerate the dogfood copy and confirm only the new step
changed:
`go run ./cmd/frit skills . --force --via "go run ./cmd/frit"`,
`git diff --stat .claude/skills/plan-new/SKILL.md`.

**Guard the edges.** Edit the skill only through its canonical file,
never the dogfood copy directly — a hand-edit there is exactly the
drift `TestDogfoodCopiesMatchCanonical` exists to catch. The install
command touches only files already under `.claude/skills`; confirm it
wrote nothing else. The new step must name S93 and
`docs/development.md` by their real paths, not a placeholder — the
gate reads the built `frit skills` output, not the source file, so a
stale reference would ship there too.

**Gate.**
`go run ./cmd/frit skills . --force --via "go run ./cmd/frit"` writes
the new step into the dogfood copy; reading it back confirms it names
S93 and `docs/development.md`.
`go test ./internal/skills -run TestDogfoodCopiesMatchCanonical` is
green. `mdsmith check .` is clean over `CLAUDE.md`, `plan/proto.md`
and the skill files. `go test ./...` and
`go tool -modfile=tools/go.mod golangci-lint run` stay green.
