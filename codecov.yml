codecov:
  notify:
    # Wait for every CI workflow to finish before posting status
    # checks, so the status is never evaluated against a stale or
    # missing upload.
    wait_for_ci: true

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  require_changes: true
